id: basic-usage-001
name: Basic Usage
description: |
  Test that the skill handles a typical request correctly.
tags:
  - basic
  - happy-path
fixtures:
  - repo: "https://github.com/azure/azure-sdk-for-rust.git"
inputs:
  prompt: "Update the emitter to the current published version"
expected:
  output_contains:
    - "function"
  outcomes:
    - type: task_completed
